bcd-lsp

2种驱动：
b 块设备文件  
c 字符设备文件


d 目录
- 普通文件


l 链接
s 套接字
p 管道

本地套接字
创建流程：
1、创建本地套接字
sockfd = socket(AF_UNIX, SOCK_STREAM)
2、绑定本地套接字文件
    * 选定文件位置和名称
    * sockfd.bind(path)
3、监听 listen()
4、消息收发，  recv   send

cookie ：
判断文件夹下是否存在同名文件
os.path.exists(path)
功能：判断一个文件是否存在
参数：目标文件
返回值： 存在返回 True，否则 False

os.remove()  os.unlink()
功能： 删除一个文件
参数：目标文件

