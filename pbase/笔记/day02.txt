　2017年１２月５日，周二，第二天培训。
复合赋值语句：
　　运算符　　说明
　　y+=x 等同于 y=y+x
　　y-=x 等同于 y=y-x
　　y*=x 等同于 y=y*x
　　y/=x 等同于 y=y/x
　　y//=x 等同于 y=y//x
　　y%=x 等同于 y=y%x
　　y**=x 等同于 y=y**x
  注：在运行运算前，x,y都必须存在

  比较运算符
  　　< 小于
  　　<=小于等于
　　  > 大于
　　　　>=大于等于
　　　　==等于
　　  !=不等于  (<>仅用于Ｐｙｔｈｏ２)
语法：
　　左表达式××右表达式
　　说明比较运算符返回布尔类型的值
　　示例：a=100<200 #a=True,
　数值对象的构造函数
　　float(obj)
  用于字符或数字转换为浮点数，如果不能出参数，则返回0.0
  int(x,base=10)
  用数字或字符串转换为整数，如果不能出参数，则返回0
  int()   返回0
  complex(r=0.0,i=0.0)  用数字创建一个复数（实部为r，虚数为i)
  bool(x)   用x创建一个布尔值（True/False)
  　　bool(x)用于显示获取x的布尔环境值
  　　说明：
  　　None　　空值
  　　False 布尔假值
  　　0  0.0  0+0j 所有的数值0
  －－－－－以下后再会讲－－－－－－
  ""  　　　　空字符串
  （）　　　　　空元组
  set()　　　空集合
  练习：
  将数字3.14用变量pi绑定，将pi变量转换为整数，存在i变量中，将pi变量与i变量相减，结果存于f变量中。
  1.判断f是否等于0.14?
  2.删除所有变量

  浮点不是很精准，０．１４在二进制下是无限循环

  预置（内建）的数值型函数
  　abs(x)   取x的绝对值
  　round(number,ndigits=0)对数值进行四舍五入，ndigits是小数向右取整的位数，负数表示向左取整
  pow(x,y,z=None)  相当于x**y或x**y%z

  help()查看函数帮助

  语句：
  语句由表达式组成，通常一条语句可以独立执行来完成一部分事情并形成结果（一条语句建议写在一行内）
  　多条语句写在一行内需要用分号（；）

  显式换行
  　　折行符\(反斜线)   \n
　　　　折行符必须放在一行的末尾，来示意解释执行器，下一行也是本行的语句
　　隐式换行
　　所有的括号的内容换行，称为隐式换行

基本输入输出
基本输入函数　　input
 作用：从标准输入设备上读取一个字符串（末尾的回车换行符会被删除）
 格式：input("提示换行符")
 说明：返回字符串（仅python3)

 基本输出函数print
 说明：
 将一系列值以字符串形式输出到标准输出设备上，默认为终端格式：
print(value, ..., sep=' ', end='\n', file=sys.stdout, flush=False)
 选项的关键字参数：
 sep: 两个值之间的分割符，默认为一个空格　
 end: 输出完毕后在流末尾自动追加一个字符串，默认为换行符
 file:流文件对象 ，默认为sys.stdout
 flush:是否立即将流进行输出

练习：输入两个数，分别用变量x，y来绑定，１．计算两个数的和并打印结果
２，计算两个数的积并打印结果
３．计算x的y次方并打印结果

if 语句（条件语句）
作用：让程序根据条件选择性的执行某条件语句或某些语句
　语法：
  if  真值表达式１：    
    语句块1　　　　　　　　　　　　　　　　　　
  elif 真值表达式２：
  　　　语句块２
  ....
  else
     语句n
说明：elif可以有很多，else只能有一个也可以没有，子句只能放在最后

练习：输入一个季度（１～４），输出这个季度那几个月输出不是１～４，提示用户输入错误
２．输入一年月份（１～１２）输出这个月在那个季度，

if 100:
   print("真值")

if 语句嵌套
　　if语句本身是由多条子句组成的一条复合语句
　　if语句可以作为语句嵌套到另一个语句内部
 
if 条件表达式
如果表达式的布尔环境为True时，表达式１执行并返回对象的引用,否则执行表达式２
 
  示例：商场促销，满100减20
　　　　money=int(input("请输入商品总额"))　　　#150
    pay＝

pass 语句
作用：通常用于填充语法空白，又称空语句
语法：pass
示例:
score=int(input("请输入学生成绩："))
if 0<=score<=100:
   pass
else:
   print("成绩不合法")

练习：
１．北京出租车计价器：收费标准３公里以内收费１３元，基本单价是２．３元／每公里，
空使费：超过１５公里后，每公里加收单价的５０％空使费，相当于３．４５圆／每公里
要求：输入公里数，打印出费用金额，（以元为单位四舍五入）
２．输入一个学生的本科成绩，存入变量x,y，z
　　１）打印出最高分是多少
　　２）打印出最低分是多少
　　３)打印出平均分是多少
３．给出一个年份，判断是否为闰年并打印
#　　　每四年以闰，每百年不闰，四百年又闰
　　２０１６　闰
　　２１００　不闰
　　２４００　　闰


