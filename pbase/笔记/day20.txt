2017年１２月２９日　　星期五　　第２０天
思考：
在学生管理项目中，每个学生有三个属性，姓名，年龄，成绩，
能否在有最小改动的情况下，再添加一个属性：家庭住址

面向对象开发，可以用继承/派生机制来实现添加新功能
继承（inheritance)和派生(derived)
继承的目的是延续旧的类class的功能，
派生目的是在旧的类基础上添加新的功能

作用：
可以将一些共有功能加载在基类中，实现代码共享（代码重用）
在不改变超类的代码基础上，改变原有的功能

继承/派生名词：
基类（base class)/／超类(super class)/父类（father class)
派生类（derived class)/子类(child class) 

单继承的语法：
class 类名（超类名）：
　　　...
任何类都直接或间接的继承自object类
object类是一切类的超类（祖类）

__base__属性：（只能用在类里面）
作用：用来记录此类的基类

覆盖：override
覆盖是指在有继承关系的类中，子类实现了与基类（超类）同名的方法，
在子类实例调用该方法时，实际调用的是子类中的覆盖版本，这种现在叫覆盖
super函数：
super(type,obj)  返回绑定超类的实例（要求obj必须为type类型的实例）
super()          返回绑定超类的实例，等同于super(__class__,self)
                 此函数必须在方法内调用

super(B,self).mymethod()

用于类的函数：
issubclass(cls,class_or_tuple)
判断一个类是否继承自其他类，如果此类cls是class或tuple中的一个派生子类，
则返回True，否则返回False

封装　enclosure
封装的目的是让使用者通过尽可能少的关心和使用变量名，
类的封装是隐藏类的实现细节，让使用者不关心这些细节
python 不能实现c++完全意义的封装（是模拟的封装）

私有实例变量和方法;
python类中，以双下划线开头，不以双线划线结尾的标识符为私有成员（实例变量或方法）
２．私有实例变量和方法只能在类内使用，不能在类外使用

显示调用基类的构造方法
当子类有自己的构造方法时，基类的构造方法将不会被自动调用
通常需要

多态polymorphic:

字面意思是多种状态
多态是指在有继承/派生关系的类中，调用基类对象的方法，实际能调用
子类的覆盖方法的现象叫多态

python的全部对象都只有‘运行时状态（动态）’，没有“c++/java"
里的，编译时状态（静态）

面向对象的编程语言的特征：
继承/派生
封装（类和私有变量）
多态

多继承multipple inheritance
多继承是指一个子类继承两个或两个以上的基类：

多继承的语法：
class 类名（超类名１，超类名２[,...])

多继承的问题：
标识符（名字空间）冲突的问题
　　要谨慎使用多继承
　
PEP8编码规范：
１．使用四个空格缩进，不使用Tab，不允许使用Tab和空格混合
２．每行最大长度７９字节，超过的部分使用反斜杠拆行
３．类和全局函数的定义间隔两个空行，类内方法定义间隔一个空行，其他地方不加空行

文档编排：
１．import
部分，按标准，三方，自己定义顺序依次排序，之间空一行
２．不要在一句import中导入多个模块，如：不推荐import
 os,sys,time
3.

空格的使用：
１．各种在括号前不用加空格
２．逗号，冒号，分号前不要加空格
３．函数左括号前不要加空格如　func　(1)
4.序列的左括号前不要加空格，如lst　[2]
5.操作符左右个加一个空格，不要为了对顺序加空格
６．函数默认参数使用赋值符左右省略空格
７．不要将多条语句在同一行，尽管可以使用“；”分隔
８．if/for/while语句中，即使执行语句只有一句，也必须
另起一行


AutoPep8 快捷键
ctrl+shift+8

练习：
实现两个文件的复制，要求用with语句，打开和关闭文件
在复制文件时，要处理所有的异常状态
