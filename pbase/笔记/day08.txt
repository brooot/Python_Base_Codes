2017年１２月13日　　星期三　　第８天

集合　set
集合是可变的容器
集合内的数据对象都是唯一的（不能重复多次的）
集合是无序的存储结构，集合中的数据关系没有先后关系
集合内的元素必须是不可变对象
集合是可迭代的
集合是相当于只有键没有值的字典（键则是集合的数据）

创建空集合：
　　set()
创建非空集合：
　　s={1,2,3,4,5}
  s=set(range(1,6))

集合的构造函数
set()   创建一个空集合（不能用｛｝来创建空集合）
set(iterable) 用可迭代对象创建一个新的集合
示例：
　s=set('ABC')  #{'A','B',"c"}
 s=set('abccba') #{'b','c','a'}
 s=set({1:'1',2:'2'})  #{1,2}

 集合的运算：
 交集　　并集，补集，子集，超集

 &生成两个集合的交集,  |生成两个集合的并集,  -生成两个集合的补集

 s1={1,2,3}
 s2={2,3,4}
 
 s3=s1&s2={2,3}
 s4=s1|s2={1,2,3,4}
 s5=s1-s2={1}       #存在s1中不存在s2中
 s6=s1^s2={1,4}      ＃生成两个集合对称补集，

大于>   判断一个集合是另一个集合的超级
小于<   判断一个集合是另一个集合的子集
 s1={1,2,3}
 s2={2,3}
 s1>s2    #True
 s1 < s2    #True 

集合in /not in  
  等同于字典，集合的特点是对于大量数，查找，删除，添加速度块

集合的内建函数：
len(x)     返回键值对的个数
max(x)
min(x)
sum(x)
any(x)   真值测试，只对键测试
all(x)
集合是可迭代对象，集合的迭代不保证先后顺序

练习：
　　经理有：曹操，刘备，周瑜
技术员：曹操，周瑜，张飞，赵云
１．即是经理又是技术员
２．是经理，但不是技术员
３，是技术员，不是经理
４．张飞是经理吗
５，身兼一职的人是谁
６．经理和技术员共几个

python3 集合中常用的方法：
s 代表集合
s.add(e)　　　在集合中添加一个新的元素e，如果元素存在，则不添加
s.remove(e) 从集合中删除一个元素，如果元素不存在于集合中，则会产生一个keyerror错误
s.discard(e) 从集合s中移除一个元素e(不会出错)
s.clear()   清空集合
s.copy()   讲一个集合浅拷贝
s.pop()    从一个集合s中删除随机元素，如果此集合为空，则引发keyerror异常
s.update(s2) 用s与s2并集更新s

集合的修改通常是先删除再加

集合推导式
　　语法：
　　｛表达式　for 变量　in 可迭代对象　[if 真值表达式]｝
示例：
将１－１０的奇数平方生成一个集合
s={x**2 for x in range(1,11)　if x%2}

固定集合　frozenset
   固定集合是不可变的，无序的，含有唯一元素的集合
作用：可以作为字典的键，也可以作为集合的值
创建空的固定的集合：
　　froxzenset()
创建非空固定集合
frozenset(iterable)
示例：frozenset(range(10))=

固定集合的方法:
  相当于集合的全部方法去掉修改集合的方法

　　　　　　　　　　　　　　　　　　　
　　　　　　　　　　　　　　　　　　　　　第一阶段总结
１．数据类型:
不可变：bool,float,complex,str,tuple,frozenset
可变的：list,dict,set,bytearray

值：None,True,False
运算符：
+-=**,*,/,//,<,>,==,is,is not,in,not in,not,and,or,
&,|,^,<<,>>,+(正号)，-（负号）

2.表达式
全部的推导式[x for x in range(10)]

语句
１．表达式语句print('hello')
２．赋值语句　　x=100,     x,y=100,200, a=b=c=1000,
3.if 语句
4.for　　语句
5.while　语句
6.del　语句
7.break　语句
8.comtinue　语句
9.pass　语句

3.内建函数：len(x),max(x),min(x),sum(x),any(x),all(x)
bool(x), int(x), float(x), complex(x), str(x), list(x), tuple(x), dict(x), set(x), frozenset(x),abs(x),round(x) range(x), bin(x), oct(x), hex(x), chr(x), ord(x),print(),input()
查找所有内建函数>>>dir(__builtins__)


函数　function
什么是函数：
　　函数是可以重复执行的语句块，可以重复使用，可以提高代码的重（chong）用性
函数的作用：
　　定义用户级别的函数
def 语句（函数定义define语句）的语法
　　　　def 函数名（参数列表）：
　　　　　　　　语句块
语法说明：
　　函数名字就是语句块的名称
　　函数名的命名规则与　变量名相同（函数名必须为标识符）
　　函数名是一个变量（不要轻易对其赋值）
　　函数有自己的名字空间，要让函数处理外部数据需要用参数给函数传入一些数据
　　函数如果不需要传入数据，则参数列表可以为空
　　语句部分不能为空，如果为空，需要用pass语句填充
示例:
def say_hello():
    print('hello')
    print('word')
函数调用：
　　函数名（实际调用传递参数）
　　＃实际调用传递参数　　简称为实参
函数调用说明：
　　函数调用是一种表达式
　　如果没有return语句，函数执行完毕后返回None值对象
　　如果函数需要返回其他的值对象需要用到return 语句
def myadd(a,b):
    print('a=',a)
    print('b=',b)

return 语句：
　　语法：
　　return[表达式]
　　注：[]代表可以省略
作用：结束当前函数的执行，返回到调用该函数的地方，同时返回一个对象的引用关系

return语句说明：
１．return 语句后跟的表达式可以省略，省略后相当于return None
2.如果函数内没有return语句，则函数执行完最后一条语句后返回None（相当于在最后加了一条
return None　语句）
３．函数的调用能够返回一个对象的引用关系


练习：
１，写一个函数，mymax,实现返回三个数的最大值，如
def mymax(a,b,c):

(要求不允许使用max函数)
２．定义两个函数：
sum3(a,b,c)  用于返回三个数和
pow3(x)　　　　返回x的三次方
１）计算１的立方＋２的立方＋３的立方和
２）计算１＋２＋３的和的立方，即（１＋２＋３）**3
３．修改学生管理项目的代码，
）１．将输入部分封装成为函数，调用函数来获取数据
）２．将打印表格封装成为函数，用打印函数打印表格
（要求多使用局部变量少使用全局变量）

